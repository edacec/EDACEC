# interface_playground

This folder is the **interface / interpretation playground**.

It must only **read** measurement outputs from `../measurement_locked/runs/outputs/`.

Run:
python3 interpret.py
